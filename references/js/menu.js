$('.menu-icon').on('click', function(e) {
  e.preventDefault();
  $('.menu').toggleClass('menu_active');
  $('section').toggleClass('section_active');
})
 $('.menu').toggleClass('menu_active');
 $('section').toggleClass('section_active');
